procs:
  backend:
    cmd: cd backend && uv run uvicorn src.main:app --reload --port 8765
    env:
      PYTHONUNBUFFERED: "1"
    cwd: .

  desktop:
    cmd: cd desktop && bun run dev
    cwd: .
    depends_on:
      - backend
