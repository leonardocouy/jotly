procs:
  backend:
    cmd: ["sh", "-c", "cd backend && uv run uvicorn src.main:app --reload --port 8765"]
    env:
      PYTHONUNBUFFERED: "1"

  desktop:
    cmd: ["sh", "-c", "cd desktop && bun run dev"]
    depends_on:
      - backend
